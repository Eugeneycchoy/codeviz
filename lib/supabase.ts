/**
 * Typed Supabase client (browser and server).
 * Create createBrowserClient / createServerClient or single client here.
 * No DB connection or queries — stub only.
 */

// Placeholder: export typed client when Supabase is wired.
export const supabase = null as unknown as ReturnType<typeof import("@supabase/supabase-js").createClient>;
